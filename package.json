{
  "name": "ai-job-copilot-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently -n web,api \"npm run dev --workspace frontend\" \"npm run dev --workspace backend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "test": "npm test --workspace backend && npm test --workspace frontend",
    "seed": "npm run seed --workspace backend",
    "typecheck": "npm run build --workspace backend && npm run build --workspace frontend",
    "cleanup:resumes": "npm run cleanup:resumes --workspace backend",
    "cleanup:profiles": "npm run cleanup:profiles --workspace backend",
    "smoke:live": "powershell -ExecutionPolicy Bypass -File scripts/smoke-live.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
