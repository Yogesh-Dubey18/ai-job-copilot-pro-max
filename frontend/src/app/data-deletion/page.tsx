export default function DataDeletionPage() {
  return (
    <main className="min-h-screen bg-slate-50 px-4 py-12 text-slate-950 sm:px-6 lg:px-8">
      <section className="mx-auto max-w-3xl rounded-lg border border-slate-200 bg-white p-8 shadow-sm">
        <h1 className="text-5xl font-black tracking-tight">Data Deletion</h1>
        <p className="mt-6 leading-8 text-slate-600">
          Signed-in users can export data or delete their account from Settings. Deleting an account removes resumes and application records from the active database.
        </p>
      </section>
    </main>
  );
}
